<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Pokémon Wiki</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
      crossorigin="anonymous"
    />
    <link rel="stylesheet" href="/css/style.css" />
  </head>
  <body>
    <%- include('partials/header') %>

    <main class="container my-4">
      <% if (success && success.length) { %>
        <div class="alert alert-success"><%= success %></div>
      <% } %>
      <% if (error && error.length) { %>
        <div class="alert alert-danger"><%= error %></div>
      <% } %>

      <div class="text-center mb-5">
        <h1 class="display-4 mb-3">Pokémon Wiki</h1>
        <p class="lead mb-4">Przeglądaj, dodawaj, edytuj i usuwaj Pokémony z hasłem na każdy wpis.</p>
        <a href="/pokemon" class="btn btn-primary btn-lg me-2">Lista Pokémonów</a>
        <a href="/pokemon/new" class="btn btn-success btn-lg">Dodaj Pokémona</a>
      </div>

      <div class="row text-center">
        <div class="col-md-4 mb-3">
          <div class="card h-100">
            <div class="card-body">
              <h5 class="card-title">Przeglądaj</h5>
              <p class="card-text">Filtruj i sortuj Pokémony po typie i numerze.</p>
              <a href="/pokemon" class="btn btn-outline-primary">Przejdź do listy</a>
            </div>
          </div>
        </div>
        <div class="col-md-4 mb-3">
          <div class="card h-100">
            <div class="card-body">
              <h5 class="card-title">Zarządzaj wpisami</h5>
              <p class="card-text">Dodawaj, edytuj i usuwaj Pokémony bez konta użytkownika.</p>
              <a href="/pokemon/new" class="btn btn-outline-success">Dodaj Pokémona</a>
            </div>
          </div>
        </div>
        <div class="col-md-4 mb-3">
          <div class="card h-100">
            <div class="card-body">
              <h5 class="card-title">Hasło do Pokémona</h5>
              <p class="card-text">Przy dodawaniu możesz ustawić hasło, które będzie wymagane
                przy późniejszej edycji lub usuwaniu tego Pokémona.</p>
              <a href="/pokemon/new" class="btn btn-outline-warning">Dodaj z hasłem</a>
            </div>
          </div>
        </div>
      </div>
    </main>

    <%- include('partials/footer') %>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" crossorigin="anonymous"></script>
    <script src="/js/script.js"></script>
  </body>
  </html>
